# Aula 07 - Re-Treino Automático
# Instalar: pip install -r requirements.txt
#
# Nota: apache-airflow vem da imagem oficial do Airflow (Aula 05).
#       Aqui listamos só o que o aluno precisa instalar localmente.

scikit-learn==1.3.0
pandas==2.0.3
numpy==1.24.3
joblib==1.3.2
mlflow==2.8.1
pytest==7.4.0
pytest-cov==4.1.0
